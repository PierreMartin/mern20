{
  "name": "server",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "cross-env": "^7.0.2",
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "path": "^0.12.7"
  },
  "scripts": {
    "server": "cross-env NODE_ENV=development nodemon server/index.js --exec babel-node",
    "server:old": "cross-env NODE_ENV=development webpack --config webpack.config.js -- --env.server && nodemon server/index.js --exec babel-node",
    "server:studento": "cross-env NODE_ENV=development nodemon webpack --config ./webpack/webpack.config.js -- --env.server && node compiled/server.dev.js"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
